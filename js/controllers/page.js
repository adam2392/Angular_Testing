/**
 * Controller: PageCtrl
 */
angular.module('DataEntry')
  .controller('PageCtrl',
    function PageCtrl ( $scope ) {
      'use strict';
      console.log('Inbox Loaded!')
      // $scope.meta = {
      //   title: "Instructions to Enter Clinical Data"
      // };
    });